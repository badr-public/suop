server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false

spring:
  application:
    name: 'eureka'
  profiles:
    active: default

#  cloud:
#    config:
#      uri: http://127.0.0.1:8888