spring:
  application:
    name: 'config'
  cloud:
    config:
      server:
        encrypt:
            enabled: false
        git:
          uri: https://github.com/badr-public/suop-config
          search-paths: common-data, mail, gw, eureka